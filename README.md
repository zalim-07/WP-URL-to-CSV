# WP URL to CSV

**WP URL to CSV** — a simple WordPress plugin to export site URLs (posts and pages) to **CSV** or **XML**.

## Features

- Exports all published **posts and pages**.
- Choose export **format**: CSV or XML.
